import type { Metadata } from "next";
import Image from "next/image";
import Link from "next/link";
import { ButtonLink, CardGrid, InfoCard, PageHero, Section } from "@/components/PagePrimitives";
import { epk, epkPressItems } from "@/lib/data";
export const metadata: Metadata = { title: "Electronic Press Kit", description: "Electronic Press Kit for Tyler Carson, Fiddler on the Rock, including bio, press photos, logos, and selected coverage." };
export default function EPKPage() { return <><PageHero eyebrow="Electronic Press Kit" title={epk.headline} subtitle={epk.subheadline + " - " + epk.kicker} image={epk.jumpImage} ctas={[{ label: "Press Coverage", href: "#coverage" }, { label: "Contact", href: "/contact" }]} /><Section eyebrow={epk.location} title="One Man Symphony"><div className="feature-split"><div className="feature-image"><Image src={epk.image} alt="Tyler Carson Fiddler on the Rock press image" fill sizes="(max-width: 768px) 100vw, 50vw" className="image-cover" /></div><div className="feature-copy"><p className="eyebrow">{epk.date}</p>{epk.bio.map((paragraph) => <p key={paragraph}>{paragraph}</p>)}<div className="button-row left"><ButtonLink link={{ label: "Book Tyler", href: "/booking" }} /><ButtonLink variant="ghost" link={{ label: "CBS Feature", href: "/cbs" }} /></div></div></div></Section><Section id="coverage" eyebrow="Press" title="What the world is saying." tone="soft"><CardGrid>{epkPressItems.map((item) => <Link key={item.title} href={item.href} target={item.href.startsWith("http") ? "_blank" : undefined} rel={item.href.startsWith("http") ? "noopener noreferrer" : undefined} className="card-link"><article className="info-card">{item.logo ? <div className="press-logo"><Image src={item.logo} alt={item.outlet + " logo"} width={180} height={80} /></div> : <p className="press-wordmark">{item.outlet}</p>}<p className="card-eyebrow">{item.outlet}</p><h3>{item.title}</h3><p>{item.summary}</p><span className="text-link">Read</span></article></Link>)}</CardGrid></Section><Section eyebrow="Downloads" title="Press-ready assets"><CardGrid>{epk.downloads.map((item) => <InfoCard key={item.label} title={item.label} body="Open the asset in a new tab for review or download." href={item.href} cta="Open" />)}</CardGrid></Section></>; }