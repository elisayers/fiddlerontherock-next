import type { Metadata } from "next";
import { CardGrid, InfoCard, PageHero, Section } from "@/components/PagePrimitives";
import { musicItems, socialLinks } from "@/lib/data";
export const metadata: Metadata = { title: "Tyler Carson Music", description: "Music by Tyler Carson, Sedona violinist and violin looping artist behind Fiddler on the Rock." };
export default function MusicPage() { return <><PageHero eyebrow="Music" title="The sound behind the Red Rocks." subtitle="Original music, looping violin, and concert pieces that shape the Living Music experience." image="/images/tyler-performance.jpg" /><Section title="Living Music Discography"><p className="lede">Explore Tyler Carson's original compositions and arrangements, inspired by the quiet beauty of the desert landscape.</p><CardGrid>{musicItems.map((item) => <InfoCard key={item.title} title={item.title} body={item.detail} />)}</CardGrid></Section><Section eyebrow="Streaming" title="External platforms" tone="soft"><CardGrid>{socialLinks.filter((item) => ["Spotify", "Apple Music", "YouTube"].includes(item.label)).map((item) => <InfoCard key={item.label} title={item.label} body={`Listen to Tyler Carson on ${item.label}.`} href={item.href} cta="Open" />)}</CardGrid></Section></>; }