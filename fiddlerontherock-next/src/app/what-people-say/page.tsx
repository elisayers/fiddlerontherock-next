import type { Metadata } from "next";
import { CardGrid, InfoCard, PageHero, Section } from "@/components/PagePrimitives";
import { reviews } from "@/lib/data";
export const metadata: Metadata = { title: "What People Say", description: "Ratings and reviews for Fiddler on the Rock from Google, Tripadvisor, Facebook, and Yelp." };
export default function ReviewsPage() { return <><PageHero eyebrow="Reviews" title="What People Say" subtitle="Read testimonials and reviews from guests who have experienced Fiddler on the Rock." image="/images/red-rock-concert.jpg" /><Section title="Ratings snapshot"><CardGrid>{reviews.map((item) => <InfoCard key={item.platform} eyebrow={item.platform + " - " + item.rating} title={item.count} body={'"' + item.quote + '" - ' + item.author} />)}</CardGrid></Section></>; }